# Nginx 反向代理配置
# 放置位置: /etc/nginx/sites-available/stock-api
# 启用: sudo ln -s /etc/nginx/sites-available/stock-api /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name api.us.stock.example.com;  # 替换为你的域名

    # 重定向到 HTTPS (可选，推荐)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持 (如果需要)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS 配置 (获取证书后启用)
# server {
#     listen 443 ssl http2;
#     server_name api.us.stock.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/api.us.stock.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.us.stock.example.com/privkey.pem;
#
#     # SSL 优化
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
